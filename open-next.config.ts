// OpenNext Cloudflare configuration.
// Keep this file dependency-free: the build installs the Cloudflare adapter locally first.
export default {
  enableCacheInterception: false,
  routePreloadingBehavior: "none",
};
